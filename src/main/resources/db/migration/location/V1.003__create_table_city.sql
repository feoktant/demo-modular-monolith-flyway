CREATE TABLE location.city
(
    city_id     INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    city        TEXT                                                            NOT NULL,
    country_id  INTEGER REFERENCES location.country ON UPDATE CASCADE ON DELETE RESTRICT NOT NULL,
    last_update TIMESTAMPTZ DEFAULT now()                                       NOT NULL
);

CREATE INDEX ON location.city (country_id);
